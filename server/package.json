{
  "name": "alakir-gallery-server",
  "version": "2.0.0",
  "description": "Optional Node backend for Alakir Gallery — admin sync, image upload, iCount IPN receiver",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
